kernelName="$(uname -s)"


alias vi="vim"
alias rm='echo "This is not the command you are looking for."; false'

case $kernelName in
Linux*)
  alias ll='ls -alFtr --color=auto'
  ;;
Darwin*)
  alias ll='ls -alFtrG'
  alias startdt='cd ~/StartDT'
  ;;
esac
