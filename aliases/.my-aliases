alias vi="nvim"
alias rm='echo "This is not the command you are looking for."; false'
alias tp="trash-put"


kernelName="$(uname -s)"

case $kernelName in
Linux*)
  alias ll='ls -alFtr --color=auto'
  ;;
Darwin*)
  alias ll='ls -alFtrG'
  ;;
esac
